<van-cell-group>
  <van-field placeholder="请输入" border="{{ true }}" model:value="{{name}}" label="公司名称" />
  <van-field placeholder="请输入" border="{{ true }}" model:value="{{position}}" label="招聘岗位" />
  <van-field placeholder="请输入" border="{{ true }}" model:value="{{base}}" label="地址" />
  <van-field placeholder="请输入" border="{{ true }}" model:value="{{contact}}" label="联系人" />
  <van-field placeholder="请输入" border="{{ true }}" model:value="{{mobile}}" label="联系电话" />
  <van-field placeholder="请输入" border="{{ true }}" model:value="{{salary}}" label="薪资" />
  <van-field placeholder="请输入" border="{{ true }}" model:value="{{info}}" label="公司介绍" />
  <van-field placeholder="请输入" border="{{ true }}" model:value="{{requirements}}" label="岗位要求" />
</van-cell-group>

<view class="pbxb fixed-bottom">
  <van-button bindtap="edit" color="linear-gradient(to right, #4bb0ff, #6149f6)" size="large" icon="records">修改信息</van-button>
</view>